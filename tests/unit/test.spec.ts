import { expect } from 'chai';

describe('Test UnitTest', () => {
  it('just an example unit test', () => {
    expect(true).to.equal(true);
  });
  it('just an example unit test', () => {
    expect(true).to.equal(true);
  });
});
